# Gradle configuration
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# Project properties
projectGroup=com.vyshali
projectVersion=1.0.0-SNAPSHOT

# Database defaults (can be overridden via command line -P flags)
dbUrl=jdbc:postgresql://localhost:5432/fxanalyzer
dbUser=postgres
dbPassword=postgres
